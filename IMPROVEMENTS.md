# لیست بهبودهای CozyTavern

> تاریخ: 2026-08-05 — لیست کامل موارد پیشنهادی برای بهبود فیچرها، UI/UX و قابلیتهای جدید.
> اولویت‌ها: ⭐⭐⭐ = حیاتی/بحرانی | ⭐⭐ = مهم | ⭐ = خوبه ولی اولویت پایین‌تر

---

## 🚀 قابلیت‌های جدید (Feature)

| # | قابلیت | توضیح | اولویت |
|---|--------|--------|--------|
| 1 | **Quick Reply / Slash Commands** | `/new`, `/continue`, `/rename`, `/settings` مثل SillyTavern — placeholder در MessageInput.tsx میگه `/? for help` ولی هیچ slash command ای پیاده نشده | ⭐⭐⭐ |
| 2 | **توکن‌کاونتر در Input** | نمایش تعداد کاراکتر/توکن هنگام تایپ (تابع `estimateTokens` در MessageBubble.tsx از قبل هست — فقط استفاده نشده) | ⭐⭐ |
| 3 | **محاسبه مجموع توکن‌ها در Header چت** | مثل SillyTavern: `{current}/{max}` — خیلی کاربردی برای مدیریت context | ⭐⭐ |
| 4 | **مشاهده و مدیریت swipes** | منوی کشویی که همه نسخه‌های یک پیام رو نشون بده و بشه انتخاب کرد (الان فقط arrow چپ/راسته) | ⭐⭐⭐ |
| 5 | **قابل اجرا بودن اولین پیام** | دکمه Play کنار `first_mes` تا کاراکتر پیام اولش رو در چت جدید بفرسته | ⭐⭐ |
| 6 | **API Keyها به‌صورت Encrypted یا Environment Variable** | رمزنگاری با key محلی یا درخواست از کاربر | ⭐⭐⭐ |
| 7 | **فعال‌سازی API برای چت‌های جاری** | از ورود به چت به‌راحتی به تنظیمات API برسیم | ⭐ |
| 8 | **چند Provider به‌صورت هم‌زمان** | پروفایل‌های مختلف (OpenAI, Ollama, etc.) با سوییچ سریع | ⭐⭐ |
| 9 | **پرامپت‌های آماده (Presets)** | قالب‌های system prompt برای ژانرهای مختلف | ⭐⭐ |
| 10 | **دکمه کپی پیام** | کپی با یک کلیک روی هر پیام | ⭐ |
| 11 | **صادرات/ایمپورت JSON** | برای کاراکتر، پرسونا و لوربوک (فرمت شناخته‌شده character card) | ⭐⭐ |
| 12 | **چند نفر — چند شخصیت هم‌زمان (Group Chat)** | چت با چند کاراکتر با هم | ⭐⭐⭐ |
| 13 | **جستجوی درون چت** | جستجوی پیام‌ها و پرش به موقعیت | ⭐⭐ |
| 14 | **قفل کردن پیام** | جلوگیری از ادیت/حذف پیام‌های مهم (SillyTavern داره) | ⭐ |
| 15 | **متنوع‌سازی صدا (Voice/Sound) برای پیام‌ها** | پخش صدای جیرینگ هنگام پیام جدید | ⭐ |
| 16 | **قابلیت drag-and-drop فایل در چت** | آپلود تصویر به پیام | ⭐⭐ |
| 17 | **افزودن مولفه‌های شخصیت** | مثلا «نام مستعار»، «قلب‌ها»، «علاقه‌ها» | ⭐ |
| 18 | **Character Import/Export** | در فرمت Character Card (PNG متادیتا) | ⭐⭐⭐ |
| 19 | **بهبود موتور لوربوک** | جستجوی **regex**، **phrase match** و **case-insensitive** که الان نداره | ⭐⭐ |
| 20 | **پیش‌نمایش لوربوک فعال** | در Right Panel نشون بده کدوم entries در پیام جاری فعال شدن | ⭐⭐ |

## 🔧 بهبود UI/UX

| # | بهبود | توضیح | اولویت |
|---|--------|--------|--------|
| 21 | **بزرگ‌نمایی متن چت (Font Size)** | اسلایدر در TopBar یا Settings | ⭐⭐⭐ |
| 22 | **تغییر درست تم (Theme)** | تم‌ها واقعی تغییر کنن نه فقط visual (الان دکمه‌های Dark/Darker/Light در Sidebar فقط ظاهرن، کاری نمی‌کنن) | ⭐⭐⭐ |
| 23 | **تایپ فارسی در ویرایشگرها** | الان چت LTR/فارسی درست کار می‌کنه ولی ورودی‌ها RTL نیستن | ⭐⭐ |
| 24 | **RTL برای متن پیام‌ها** | تشخیص خودکار RTL/LTR برای متن هر پیام | ⭐⭐⭐ |
| 25 | **کاهش ظاهر شدن دکمه‌ها با delay** | دکمه‌های hover الان خیلی سریع ظاهر می‌شن، delay 100-150ms بذار | ⭐⭐ |
| 26 | **Tooltip فارسی برای همه آیکون‌ها** | الان `title` ها انگلیسی و گاهی غایبن | ⭐⭐ |
| 27 | **اسکرول نرم و sticky buttons** | استیکی کردن header چت، بهبود اسکرول | ⭐ |
| 28 | **افزودن تاریخ جداکننده بین روزها** | در لیست پیام‌ها | ⭐⭐ |
| 29 | **پیش‌نمایش کاراکتر قبل از انتخاب** | در gallery | ⭐ |
| 30 | **موارد اخیر (Recents) در بالای سایدبار** | دسترسی سریع به کاراکترهای اخیر | ⭐ |
| 31 | **منوی زمینه‌ای (Context Menu) روی پیام‌ها** | کپی/ادیت/برنچ/حذف — الان دکمه‌های جداگانه hover هستند | ⭐⭐⭐ |
| 32 | **کنترل‌چرخه swipe از صفحه‌کلید** | مثلاً Alt+Arrow | ⭐ |
| 33 | **Mobile: کشیدن با انگشت برای swipe** | به جای کلیک روی آیکون | ⭐⭐ |
| 34 | **نمایش تعداد swipes با شماره** | `3/7` روی هر پیام | ⭐ |
| 35 | **حالت فوکوس (Focus Mode)** | مخفی کردن همه پنل‌ها برای تمرکز روی چت | ⭐⭐ |
| 36 | **جستجوی کاراکترها در سایدبار با تگ** | فیلتر با تگ | ⭐ |

## ⚙️ بهبودهای فنی (Server)

| # | بهبود | توضیح | اولویت |
|---|--------|--------|--------|
| 37 | **بهبود پارس SSE** | بافر برای chunk های ناقص به‌جای `split('\n')` ساده در app.ts | ⭐⭐⭐ |
| 38 | **از سرگیری (Resume) پاسخ ناقص** | اگر وسط پاسخ قطع شد، از آخرین token ادامه بده | ⭐⭐⭐ |
| 39 | **کنترل بک‌پرس (Backpressure)** | اگر کلاینت کندتر از سرور باشه، بافر مموری پر نشه | ⭐⭐ |
| 40 | **Abort در سمت کلاینت** | اگر کاربر Cancel بزنه، درخواست به سرور هم لغو بشه — الان فقط UI قطع می‌شه ولی سرور ادامه می‌ده | ⭐⭐⭐ |
| 41 | **خطای بهتر برای API ها** | نشون دادن status و پیام اصلی در خطا | ⭐⭐ |
| 42 | **Rate limiting** | جلوگیری از سوءاستفاده | ⭐ |
| 43 | **ذخیره سمت سرور از پاسخ‌های streaming** | در صورت قطع شدن، حداقل پیام partial ذخیره بشه | ⭐⭐⭐ |
| 44 | **فایل جداگانه برای app/chat route** | تست‌پذیر کردن و جدا کردن منطق چت | ⭐⭐ |

## 📦 بهبودهای فنی (Client)

| # | بهبود | توضیح | اولویت |
|---|--------|--------|--------|
| 45 | **کاهش رندر مجدد کل لیست هنگام تایپ** | MessageList الان با هر set همه پیام‌ها رو re-render می‌کنه (virtual scroll فقط DOM رو محدود می‌کنه) — این حساسه چون هر token یک set قبول می‌کنه | ⭐⭐⭐ |
| 46 | **Memoization با React.memo** | پیام‌ها و کامپوننت‌های سنگین | ⭐⭐⭐ |
| 47 | **بارگذاری تنبل برای مودال‌ها** | `React.lazy` برای CharacterEditor و بقیه | ⭐⭐ |
| 48 | **مدیریت خطای خواندن فایل آواتار** | try/catch در `handleAvatarUpload` | ⭐ |
| 49 | **حذف avatar با دکمه** | الان فقط می‌شود جایگزین کرد | ⭐ |
| 50 | **دسته‌بندی کاراکترها با تگ** | در سایدبار | ⭐⭐ |

## 🛡️ امنیت و Data Integrity

| # | بهبود | توضیح | اولویت |
|---|--------|--------|--------|
| 51 | **رمزنگاری API Key** | الان به‌صورت Plain Text در دیتابیس ذخیره می‌شه | ⭐⭐⭐ |
| 52 | **حذف کاراکترها بدون confirmation در UI چت** | در ChatView هست ولی فقط در سایدبار | ⭐⭐ |
| 53 | **یک Unique ID برای پیام‌ها** | بهتر از rely بر `send_date` (دو پیام با timestamp یکسان ترتیبشون بهم می‌ریزه) | ⭐⭐⭐ |
| 54 | **اعتبارسنجی ورودی سرور** | الان هیچ validation نداره — هرچی ارسال بشه قبول می‌شه | ⭐⭐ |
| 55 | **مهاجرت‌های دیتابیس بهتر** | الان migrations با `PRAGMA` دستی در db.ts | ⭐⭐ |
| 56 | **جلوگیری از race condition در دو پیام هم‌زمان** | مثلاً دو تب هم‌زمان به یک چت | ⭐ |

## 📊 تست و QA

| # | بهبود | توضیح | اولویت |
|---|--------|--------|--------|
| 57 | **تست برای app.ts / chat endpoint** | تست streaming, error cases, non-stream | ⭐⭐⭐ |
| 58 | **تست برای messages.ts (swipe/regenerate/edit)** | | ⭐⭐⭐ |
| 59 | **تست E2E برای client** | با Playwright یا Cypress | ⭐⭐ |
| 60 | **تست برای api-settings.ts** | | ⭐⭐ |

---

## ⚠️ مشکلات شناخته‌شده فعلی (باگ‌ها)

1. ~~**SSE پارس ناقص**~~ ✅ **رفع شد (2026-08-05)** — `app.ts` و `client.ts` از buffer خط مبنا استفاده می‌کنند
2. ~~**API Key Plain Text**~~ — هنوز بدون رمزنگاری در دیتابیس (اولویت پایین برای پروژه محلی)
3. **confirmResolver ممکنه هیچ‌وقت resolve نشه** — اگر مودال باز بشه و کاربر از بیرون کلیک کنه
4. ~~**مرتب‌سازی با `send_date`**~~ ✅ **رفع شد (2026-08-05)** — همه کوئری‌ها `ORDER BY rowid` شدند
5. **دکمه‌های تم** — غیرفعال هستن و فقط ظاهرن
6. ~~**فقدان تست روی app.ts و messages.ts**~~ ✅ **رفع شد (2026-08-05)** — chat.test.ts با ۷ تست اضافه شد (۸۵ تست کل)
7. **باگ جدید (پیداشده در تست 2026-08-05): پیام‌های جدید در UI نمایش داده نمی‌شوند وقتی scroll در پایین نیست** — در `MessageList`/virtual scroll، پیام‌های جدید در انتهای لیست رندر نمی‌شوند مگر کاربر نزدیک پایین باشد. باید اسکرول خودکار به پایین بعد از ارسال/دریافت پیام جدید تضمین شود. این باگ قبلی بود ولی در تست E2E نمایان شد.
8. **سرور با `character_id` ناموجود کرش می‌کرد** — ✅ **رفع شد (2026-08-05)** — اعتبارسنجی اضافه شد (خطای 400 به جای crash)

## 🎯 Quick Wins (کم هزینه، سریع اثر)

1. ~~**تکمیل SSE پارس**~~ ✅ انجام شد
2. **کپی پیام** — یک آیکون ساده
3. **مشاهده swipes** — منوی کشویی
4. **دکمه‌های hover با delay** — UX خیلی بهتر می‌شه
5. **کامل کردن theme switcher** — حداقل «Dark/Darker» کار کنه
6. **Token counter** در ورودی

---

## ✅ فاز ۱ — انجام‌شده (2026-08-05)

| مورد | وضعیت |
|------|--------|
| SSE parser درست (سرور + کلاینت) | ✅ `createLineBuffer` در providers.ts + buffer در client.ts |
| دکمه Stop + AbortController | ✅ `stopGeneration` در store + دکمه Stop در MessageInput + `activeStreams` و `POST /api/chat/abort` در سرور |
| ذخیره partial در قطعی | ✅ در تست E2E با mock ۶۰ ثانیه‌ای تأیید شد (توکن ۱۳ از ۳۰ ذخیره شد) |
| مرتب‌سازی پیام‌ها با rowid | ✅ تمام کوئری‌ها |
| ذخیره انتخاب فعال (پرسونا/لوربوک) در localStorage | ✅ |
| تست chat endpoint | ✅ ۷ تست جدید (streaming/abort/خطا/non-stream) |
| اعتبارسنجی کاراکتر/چت ناموجود در /api/chat | ✅ خطای 400 به جای crash |

**نکته تست E2E**: mock server محلی (پورت 4100) با node خام ساخته شد چون `setInterval` در node پس‌زمینه tsx درست flush نمی‌شد. تنظیمات API کاربر (deepseek) بعد از تست به حالت اولیه برگردانده شد.
